# 微信公众号爬虫依赖 (agent-browser 版本)

# HTTP 请求
requests>=2.28.0

# 日志
loguru>=0.6.0

# HTML 解析
beautifulsoup4>=4.11.0
lxml>=4.9.0

# HTML 转 Markdown
markdownify>=0.11.0

# 进度条
tqdm>=4.64.0

# 异步 HTTP
aiohttp>=3.8.0

# 注意：此版本使用 agent-browser CLI 进行浏览器自动化
# 需要单独安装 agent-browser:
#   npm install -g agent-browser
#   agent-browser install
